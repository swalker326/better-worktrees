---
import DocsLayout from "../layouts/DocsLayout.astro";
---

<DocsLayout title="Commands | better-worktree" description="CLI and TUI command reference.">
  <h1>Command reference</h1>
  <p class="muted">Core commands available from <code>bwt</code>.</p>

  <h2>Worktree commands</h2>
  <pre><code>bwt list [-R &lt;repo&gt;] [--json]
bwt create &lt;name&gt; [-R &lt;repo&gt;] [--branch &lt;branch&gt;] [--base &lt;base&gt;] [--path &lt;dir&gt;] [--json]
bwt delete &lt;name|path&gt; [-R &lt;repo&gt;] [--yes] [--force]</code></pre>

  <h2>Repo commands</h2>
  <pre><code>bwt repo list
bwt repo add &lt;path&gt; [--name &lt;alias&gt;]
bwt repo remove &lt;path&gt;
bwt repo use &lt;path&gt;
bwt repo set-copy &lt;path&gt; --rule &lt;from[:to]&gt; [--rule &lt;from[:to]&gt; ...]</code></pre>

  <h2>TUI keys</h2>
  <pre><code>j / k or arrows  move selection
Enter            open repo/select
a                add repo or create worktree
d                delete selected worktree
x                remove saved repo
r                refresh
s                switch to repo picker
?                help
q                quit</code></pre>
</DocsLayout>
